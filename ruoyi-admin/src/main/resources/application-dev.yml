# 业务配置
business:
  # 请求、响应数据加密
  aes-encrypt:
    # 是否启用
    enabled: false
  # 服务端根路径
  serviceUrl: http://tellsea.4kb.cn/ruoyi-vue-service
  # 用户端根路径
  appUserUrl: http://192.168.0.105:8081/ruoyi-vue-app-user/#
  # 微信配置
  weiXinMp:
    # 公众号授权登录回调地址
    callbackUrl: ${business.serviceUrl}/au/weiXinMp/callback
    # 回调完成重定向前端地址
    loginUrl: ${business.appUserUrl}/login/login-weixin-mp
# 微信配置
wx:
  mp:
    configs:
      - appId: wxfed44936431200f0
        secret: 8486ea1cf41d2fb20f81fa810b5b26e6
        token:
        aesKey:
# 日志配置
logging:
  level:
    org.springframework: info
    com.github.binarywang.demo.wx.mp: DEBUG
    org.mybatis.spring.mapper: DEBUG
    com.baomidou.mybatisplus: DEBUG
    #项目mapper目录
    com.ruoyi.business.mapper: DEBUG

    me.chanjar.weixin: DEBUG

# 数据源配置
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    druid:
      # 主库数据源
      master:
        url: jdbc:mysql://121.43.179.245:3306/sh-iot?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
        username: root
        password: 24jx
      # 从库数据源
      slave:
        # 从数据源开关/默认关闭
        enabled: false
        url:
        username:
        password:
      # 初始连接数
      initialSize: 5
      # 最小连接池数量
      minIdle: 10
      # 最大连接池数量
      maxActive: 20
      # 配置获取连接等待超时的时间
      maxWait: 60000
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
      timeBetweenEvictionRunsMillis: 60000
      # 配置一个连接在池中最小生存的时间，单位是毫秒
      minEvictableIdleTimeMillis: 300000
      # 配置一个连接在池中最大生存的时间，单位是毫秒
      maxEvictableIdleTimeMillis: 900000
      # 配置检测连接是否有效
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      webStatFilter:
        enabled: true
      statViewServlet:
        enabled: true
        # 设置白名单，不填则允许所有访问
        allow:
        url-pattern: /druid/*
        # 控制台管理用户名和密码
        login-username: admin
        login-password: 123456
      filter:
        stat:
          enabled: true
          # 慢SQL记录
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true
  # redis 配置
  redis:
    # 地址
    host: localhost
    # 端口，默认为6379
    port: 6379
    # 数据库索引
    database: 3
    # 密码
    password: redis4mac
    # 连接超时时间
    timeout: 10s
    lettuce:
      pool:
        # 连接池中的最小空闲连接
        min-idle: 0
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池的最大数据库连接数
        max-active: 8
        # #连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1ms
mqtt:
  server:
    host: tcp://39.99.180.14:1883
  client:
    id: mqttx_ea47844
  username: root
  password: root

udp:
  port: 9990
  worker-threads: 4            # Netty IO 线程数（一般 = CPU核数 * factor）
  business-threads: 64        # 处理业务的线程池大小
  receive-buffer-size: 1048576
  send-buffer-size: 1048576
  reuse-address: true
  broadcast: false
